import { Loantype2namePipe } from './loantype2name.pipe';

describe('Loantype2namePipe', () => {
  it('create an instance', () => {
    const pipe = new Loantype2namePipe();
    expect(pipe).toBeTruthy();
  });
});
