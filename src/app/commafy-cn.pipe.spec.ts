import { CommafyCNPipe } from './commafy-cn.pipe';

describe('CommafyCNPipe', () => {
  it('create an instance', () => {
    const pipe = new CommafyCNPipe();
    expect(pipe).toBeTruthy();
  });
});
