import { MillionShowPipe } from './million-show.pipe';

describe('MillionShowPipe', () => {
  it('create an instance', () => {
    const pipe = new MillionShowPipe();
    expect(pipe).toBeTruthy();
  });
});
