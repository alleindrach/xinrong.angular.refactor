import { SectionShowPipe } from './section-show.pipe';

describe('SectionShowPipe', () => {
  it('create an instance', () => {
    const pipe = new SectionShowPipe();
    expect(pipe).toBeTruthy();
  });
});
